################################################################################
# FarGan-RVQ training requirements (aligned with OSCE discriminators)
# - Pinned versions to match dnn/torch/osce/requirements.txt
# - Install PyTorch/CUDA via conda first (see README/env instructions)
################################################################################

# Core ML stack (pin to OSCE-compatible versions)
torch==2.0.1
torchaudio==2.0.2
numpy==1.25.2
scipy==1.11.2

# Audio processing
soundfile>=0.12.1
librosa==0.10.0

# Data handling & visualization
matplotlib==3.7.3
scikit-learn>=1.1.0
pandas>=1.5.0
pyyaml==6.0.1

# Training utilities
tqdm==4.66.1
tensorboard>=2.10.0
wandb>=0.13.0  # optional

# OSCE extras (for multi-resolution frequency discriminators)
pesq==0.0.4
gitpython==3.1.41

# Dev tools (optional)
pytest>=7.0.0
black>=22.0.0
isort>=5.10.0

# Optional/advanced (commented: install only if you know you need them)
# triton>=2.0.0        # custom CUDA kernels
# flash-attn>=2.0.0    # fast attention
# apex                 # NVIDIA Apex (instead of native AMP)
